<h1>Task一覧ページ</h1>
<ul>
  
<% @tasks.each do |task| %>
  <li>
  <%= link_to task.id, task %>
  <%= task.content %>&gt;<%= task.status %>
  </li>
<% end %>

</ul>

<%= link_to '新規投稿ページ', new_task_path %>